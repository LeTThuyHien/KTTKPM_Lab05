services:
  flask1:
    build: .
    container_name: flask1

  flask2:
    build: .
    container_name: flask2

  nginx:
    image: nginx:latest
    ports:
      - "8082:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - flask1
      - flask2
